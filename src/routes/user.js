const { Router } = require('express');
const router = Router();
const dashboard = require("../controller/dashboard.controller");;

//router.get("/statusalarm", dashboard.statusAlarma );
//router.get("/subsistemas", dashboard.statusSubSistemas);

module.exports = router;